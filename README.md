# first-git

## Here different codes are excuted

$ git --version

$ echo "# first-git" >> README.md
$ ls

$ git config --global user.name "username"

$ git config --global user.email "email"

$ git init

$ git add -A

$ git commit -m "first commit"

$ git branch -M main

$ git remote add origin https://github.com/dewmahmud/first-git.git

$ git push -u origin main
